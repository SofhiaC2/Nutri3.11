<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">  <!-- usa cor de destaque do tema -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/verde_claro"/> <!-- cor do fundo do botão -->
            <stroke android:width="1dp" android:color="@color/verde_claro"/>
            <corners android:radius="12dp"/> <!-- ajustar arredondamento -->
            <padding android:left="8dp" android:top="8dp" android:right="8dp" android:bottom="8dp"/>

        </shape>
    </item>
</ripple>
